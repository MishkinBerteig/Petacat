# Build and Artifact Verification

## Current Process-Novelty Revision

2026-09-07. At the author's request, the abstract and body positively identify
the integration and application of Good-Turing guidance, observed-support sets,
and p50 diagnostics as a process-level contribution. Port improvement remains
the primary engineering result. The new native-best episodic study remains
explicitly support-only; neither its results nor the historical claims change.

- The anonymous PDF remains 27 US Letter pages, including 18 main-text pages.
  All-page text bounds, empty author metadata, 35 bibliography entries, 22
  embedded fonts, and official submission-mode/style checks pass. The first
  page, related-work addition, conclusion, and page contact sheet were inspected.
  Four underfull vertical-box warnings and the existing style diagnostics remain;
  there are no overfull boxes or undefined references/citations.
- All 51 academic, 14 reconstruction, and five submission-package tests pass.
  The converter reproduces the saved-data audits and tables. All 32 packaged
  academic audit, table, analysis-tool, and test files remain byte-identical
  to the experimental ZIP, whose checksum is unchanged. Neither engine runs.
- The refreshed 21-file source ZIP builds independently with identical
  extracted PDF text. The combined Supplementary Material ZIP contains this
  updated source bundle and the unchanged experimental ZIP. Integrity,
  inventory, exact-inner-byte, and private-identifier checks pass.
- The regenerated plain-text abstract has 181 whitespace-delimited words and
  1,417 characters. The public-form limits, long-submission classification, and
  anonymous-file checks pass again; exact file sizes and hashes are in
  [submission-build.json](submission-build.json) and
  [submission-readiness.json](submission-readiness.json).
- Scientific observations, frozen code, study protocols, reference entries,
  and all thirteen generated tables are unchanged. No full numerical study
  reanalysis, experiment, commit, push, login, or submission was performed.
  Final manual author review and approval remain pending.

Current PDF SHA-256:

```text
219badf514ad7dd071dac5878caf7f93de6cbb15619dc18718439748baed4fc2
```

Current source ZIP SHA-256:

```text
e477953198edd8bc834cdfcaac3e004425a9cccbe0f4e447976651ea0c1d10d4
```

Current single Supplementary Material ZIP SHA-256:

```text
ae11bf6c894bf08ef532adcf21b294602cd183a57d7164358fe1e7e951ddbc05
```

The experimental and historical arithmetic ZIP hashes remain unchanged from
the previous audit below.

## Previous Anonymous Initial-Submission Audit

2026-09-07. Following the author's rejection of the proposed declarations
checklist, verification targets the initial anonymous OpenReview submission,
not a preprint or camera-ready manuscript. The paper and both existing inner
ZIPs remain byte-identical to Issue 7. No identifying author information was
added, and no private form responses were entered.

- The public `TMLR/-/Submission` invitation confirms one supplementary attachment,
  a 50 MB PDF limit, a 100 MB supplementary limit, and the long-submission
  category for more than 12 main-text pages. The paper has 18 main-text pages
  and 27 pages overall. Beyond PDF is a different, interactive-webpage category,
  not the destination for the LaTeX or experimental ZIPs.
- The manuscript uses the official anonymous submission mode, with neither
  `preprint` nor `accepted`. PDF author metadata is empty; text, metadata,
  hyperlinks, and intended attachments pass private-identifier scans. The
  installed style files match a fresh download from the official template URL.
  All 22 fonts are embedded, 35 references remain, appendices follow references,
  and all page text bounds pass. Existing nonfatal build diagnostics are
  unchanged from Issue 7.
- A single 77,920,470-byte supplementary ZIP contains the unchanged experimental
  and source ZIPs, an anonymous README, and an upload checksum manifest.
  Inventory, regular-file metadata, normalized timestamps, ZIP integrity,
  private identifiers, and exact inner bytes are verified. All four nested
  single-run archives separately pass checksums, inventories, safe extraction,
  and privacy scans. No upstream Metacat source or runtime binary is added.
- From a clean extraction of the actual combined attachment, the source ZIP
  rebuilds with identical extracted PDF text. The experimental verifier checks
  195 frozen-source comparisons, reproduces five audit reports and thirteen
  tables byte-for-byte, and reproduces all 25,974 episodic records' statistics.
  Its 51 academic and 14 reconstruction tests pass; five new outer-package
  tests also pass. Neither engine is executed, and the expensive experiments
  and full single-run numerical reanalysis are not rerun.
- The anonymous plain-text abstract is generated from manuscript metadata.
  Its 1,390 characters and the title's 85 characters fit the live form's limits.
  [INITIAL-SUBMISSION.md](INITIAL-SUBMISSION.md) gives exact field/file mappings;
  [submission-readiness.json](submission-readiness.json) and
  [submission-build.json](submission-build.json) record the checks and hashes.
- The initial form's author and declaration fields have restricted readers;
  this does not require deanonymizing the PDF. No account state or eligibility
  was checked, no declaration was invented, and no login or submission occurred.
  Final manual author review and approval still precede any commit or push.

Anonymous PDF SHA-256 (unchanged):

```text
acfbddf46f38974e412b187dedb60c094917c640ef4cd4eaee01f72624719a58
```

Single Supplementary Material upload SHA-256:

```text
2f8100ace78361255dc43d70d190f234d8367bb50853676edf4685078360a75a
```

The source, experimental, and historical arithmetic ZIP hashes remain as
recorded below under Issue 7.

## Previous Issue 7 Build

2026-09-07. The author-approved title is **Large References, Small Checks:
Oracle-Guided Porting of a Stochastic Learning System**. It appears in the
manuscript, PDF title and metadata, generated LaTeX, and current bundle
documentation. Historical title records remain labeled as historical. Final
manual author review and approval are still required before any commit or push.

- The manuscript diff changes exactly the title. Its abstract, scientific body,
  equations, citations, tables, and appendix prose remain unchanged. The entire
  extracted PDF text after the title is identical to Issue 6, including page
  breaks. The PDF remains 27 US Letter pages, with 18 pages of main text.
- The first page was visually inspected. All pages pass text-bound checks;
  22 fonts are embedded, all 35 references remain, and author metadata is empty.
  No undefined references/citations, missing-character warnings, or overfull
  boxes occur. Three underfull vertical-box warnings and the previously
  documented style diagnostics remain.
- All 51 academic tests pass. The source ZIP has 21 files and is 64,070 bytes;
  a clean extraction compiles with identical extracted PDF text. ZIP integrity,
  safe-path, identifying-token, and official-style comparisons pass.
- The experimental ZIP has 208 files and is 77,854,002 bytes. Only its root
  README title and inventory metadata change from Issue 6. All scientific
  evidence, analysis code, tables, frozen source files, and source-snapshot
  ledgers remain byte-identical. The builder verifies outer contents and all
  four nested single-run archives, including identifying-token checks.
- No engine experiment or full single-run numerical reanalysis was run for the
  title change. The converter regenerated the existing audits and tables without
  changing their bytes; evidence and analysis code retain their Issue 6
  verification. Original
  Metacat, study, engine, and license files are unchanged. Nothing was committed,
  pushed, or submitted.

Current PDF SHA-256:

```text
acfbddf46f38974e412b187dedb60c094917c640ef4cd4eaee01f72624719a58
```

Current source ZIP SHA-256:

```text
1c95e2c1c44e3a93474ce9df0889ebbcae9d1ac57c2b882600aa4dbec4698b62
```

Current experimental ZIP SHA-256:

```text
08cb16fee2840f73dfdd78033a38c0c85eb9ac1ceba4432c1724928d2a327713
```

The historical analysis ZIP remains 294,696 bytes with SHA-256
`c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52`.
Machine-readable package results are in
[supplement-build.json](supplement-build.json).

## Previous Issue 6 Build

2026-09-07. The author-approved editorial consolidation shortens repeated
explanations while preserving the historical repair workflow as the core
engineering result. The abstract is 177 words, down from 223. Main text occupies
18 pages rather than 19; references begin on page 19. No commit, push, or
submission is authorized before final manual author review and approval.

- PDF: 27 US Letter pages rather than 28, anonymous byline, no author metadata,
  and 22 embedded fonts. All 35 citations and bibliography entries remain.
  Every page passes text-bound checks. The full-document contact sheet, first
  page, revised conclusion, and both large episodic tables were inspected.
- Citation occurrences, section anchors, display equations, manuscript table
  rows, and numeric-token inventory match the pre-edit manuscript. Appendix
  prose is byte-identical. Semantic review confirms that qualifications remain
  either beside the results or in the explicitly cross-referenced explanation.
- The two main-text episodic tables now use the converter's existing
  keep-together option. Their captions and all data rows are unchanged, and
  both complete tables fit on page 16. The conclusion ends on page 18 without
  an orphaned continuation page. No official style file was modified.
- No undefined references/citations, missing-character warnings, or overfull
  boxes. Three underfull vertical-box warnings and the previously documented
  style diagnostics remain; this is not a warning-free build.
- The source ZIP contains 21 files and is 64,000 bytes. A fresh extraction
  compiles independently with identical extracted PDF text. ZIP integrity,
  safe-path, identifying-token, and official-style byte comparisons pass.
- The experimental ZIP contains 208 files and is 77,853,957 bytes. Compared
  with Issue 5, only the converter and the two generated table layout files
  change, plus their inventory metadata. Every scientific evidence file and
  frozen source file remains byte-identical; both source-snapshot ledgers match.
- Clean-extraction verification checks 195 frozen-source comparisons,
  reproduces all five audit reports and thirteen tables byte-for-byte, and
  reproduces all 25,974 episodic records' stopping decisions, gates, bounds,
  and frequencies. All 51 academic tests and 14 reconstruction-helper tests
  pass. The builder also verifies all four nested single-run archives.
- The complete single-run numerical reanalysis was not repeated for this
  prose/layout-only revision. Its byte-identical reproduction was verified
  under Issue 5; none of its data, analysis code, or dependencies changed.
  Neither engine was executed. Original Metacat, license, study, and engine
  files remain untouched.

Current PDF SHA-256:

```text
bf4c40bdcfc23c7fa093df9263e568afa6b8c9c58ef9c9a1dc0b518f3161e365
```

Current source ZIP SHA-256:

```text
2d36ee29bb8918e195ce9c093fe18df09af718499efee9242abf9da054cc6206
```

Current experimental ZIP SHA-256:

```text
9d9b184c409002a81cb6d8db53786d5a048d47c62ac9eed32014ef7644107e84
```

The historical analysis ZIP remains 294,696 bytes with SHA-256
`c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52`.
Machine-readable package results are in
[supplement-build.json](supplement-build.json).

## Previous Issue 5 Build

2026-09-07. The author-approved anonymous experimental supplement is built
separately from the standalone manuscript source and historical arithmetic
ZIPs. Appendix A describes its reproduction commands, evidence boundaries,
and review-specific documentation. The historical development narrative and
all scientific results remain unchanged. Final author review and approval are
still required before any commit, push, or submission.

- PDF: 28 US Letter pages, anonymous byline, no author metadata, and 22
  embedded fonts. All 35 citations and bibliography entries remain present.
  Text bounds pass on every page; the full-document contact sheet and revised
  Appendix A were visually inspected. The complete all-input port table stays
  on page 17, and the episodic appendix tables stay on page 27.
- No undefined references/citations, missing-character warnings, or overfull
  boxes. Three underfull vertical-box warnings remain, along with the previously
  documented style diagnostics. The two horizontal-box warnings from Issue 4
  are resolved. This is not a warning-free build.
- The source ZIP contains 21 files and is 65,832 bytes. A fresh extraction
  compiles independently with identical extracted PDF text. Official TMLR
  styles remain byte-identical to the downloaded template. Source/historical
  ZIP integrity, safe-path, and identifying-token checks pass.
- The experimental ZIP contains 208 files and is 77,853,971 bytes. Its outer
  inventory, hashes, normalized metadata, paths, and identifying-token checks
  pass; all four unchanged nested single-run archives pass their release
  checks. The package records 195 passing frozen-source comparisons across
  the two studies' manifests.
- From a clean extraction, all five audit reports and thirteen generated
  tables reproduce byte-for-byte. The episodic verifier reproduces all
  25,974 episodes' stopping decisions, gates, bounds, and frequencies. All
  51 academic tests and 14 reconstruction-helper tests pass.
- The clean extraction also passes `python verify.py --full-single-run`:
  the complete single-run saved-observation analysis, including its numerical
  frequency comparisons, reproduces `analysis.json` and `RESULTS.md`
  byte-for-byte for all 19 problems. Verification used Python 3.14.5 on macOS
  arm64 with the recorded NumPy 2.5.1, SciPy 1.18.1, and pytest 9.1.1; the
  original study recorded Python 3.14.6. Neither engine was executed.
- The bundled reconstruction helper verifies all 69 reconstructed Metacat
  files against the manifest using the separately obtained upstream archive.
  No upstream source, tarball, interpreter, or container image is bundled.
- Two README files have review-specific replacements. The existing MIT
  license's copyright-holder display name is temporarily anonymized, with its
  terms and upstream attribution preserved. The original license, study
  archives, study tooling, engine source, and Metacat bundle remain untouched.
- The private episodic attempt archive is excluded explicitly; public winner
  exports reproduce the statistics, not missing raw states or causal
  interventions. Scientific implementation labels and immutable hashes remain;
  direct-identifier checks do not guarantee anonymity against external matching.
  Final author-level anonymity review remains necessary.

Current PDF SHA-256:

```text
e9382dc88fe31e8766b081d73815d5a078876c1b218dd114356d391d82a07311
```

Current source ZIP SHA-256:

```text
57bcaa9b40dd767bb501893b5077e868c502fec84f47d3f91722ba1da43f7260
```

Current experimental ZIP SHA-256:

```text
9d2c7566c61d14b4a0d6d97517695d9045e8c7ac66a0d41407c72953e6b34206
```

The historical analysis ZIP remains 294,696 bytes with SHA-256
`c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52`.
Machine-readable package results are in
[supplement-build.json](supplement-build.json).

## Previous Issue 4 Build

2026-09-07. The author-approved all-input episodic port comparison is
incorporated in Section 8, with the complete table on page 17. Appendix D
(page 27) adds both populations' descriptive frequency comparisons for all
five construction-limited problems and disjoint inner-run accounting. The
historical development narrative and independently frozen supports remain
unchanged. Final author review and approval are still required before any
commit or push.

- PDF: 28 US Letter pages, anonymous byline, empty author metadata, and 23
  embedded fonts. All 35 citations and bibliography entries remain present.
- All 45 academic tests pass, including eight new saved-episode audit tests
  and two new table tests. The audit checks all 38 populations, preserves
  absent-oracle versus zero-outside distinctions, and reconciles conditional
  episode denominators and all 207,791 actual inner runs.
- The v3 export verifier reproduces all 25,974 episodes' stopping decisions,
  gates, bounds, and frequencies with zero engine executions. The new audit
  verifies saved input hashes and independently checks selection counts and
  membership summaries; it does not reconstruct native scores. No study data,
  study tooling, or engine source changed.
- Text bounding boxes fit the horizontal text area and page boundaries. The
  full-document contact sheet and detailed new-table renders were inspected.
  The all-19-input port table and both new appendix tables each remain intact.
- No undefined references/citations, missing-character warnings, or overfull
  boxes. Three underfull vertical-box and two underfull horizontal-box
  warnings remain, along with the previously documented style diagnostics;
  this is not a warning-free build.
- The source ZIP has 21 files and is 65,756 bytes, including all thirteen
  generated tables. A clean extraction rebuilds independently with identical
  extracted PDF text. The historical arithmetic ZIP remains unchanged at
  294,696 bytes and does not contain the new study audit.
- ZIP integrity, safe paths, private-identifier checks, and byte-for-byte
  official-style checks pass. Complete anonymous experimental packaging and
  author-level anonymity review remain pending.

Current PDF SHA-256:

```text
db21152998aadd8bc53f111e34938185f073ef7320fd4d8b5b32b4a95002d917
```

Current source ZIP SHA-256:

```text
354de80a04b103067c8f3d562106f68ec812b9dda808c9d0a6addbfa4a35398f
```

The analysis ZIP retains SHA-256
`c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52`.

## Previous Issue 3 Build

2026-09-07. The author-approved integration of the completed single-run study
includes the author's modification: historical development and port improvement
remain the core engineering result. The abstract, introduction, Section 6, and
conclusion emphasize that history while retaining its provenance limitations.
Section 7 and Appendix C add the versioned measurement evidence, rather than
claiming to reconstruct the historical interventions. Final author review and
approval are still required before any commit or push.

- PDF: 26 US Letter pages, anonymous byline, empty author metadata, and 23
  embedded fonts. All 35 citations and bibliography entries remain present.
- All 35 academic tests pass. The new saved-analysis audit checks the
  969,000-observation accounting, retained errors, per-input and per-batch
  counts, novel outcomes, and ordered-prefix comparisons. It reproduces
  frequency rejection counts from saved p-values; it does not rerun the
  original permutation simulations or either engine.
- All four full single-run data archives pass the release verifier's checksums,
  inventories, safe extraction, and privacy checks. Their inventories contain
  34,086 main, 20,593 parent, 270 pilot, and 20,601 aborted-preflight files.
  The episodic export verifier reproduces all 25,974 saved episodes' statistics
  with zero engine executions. No frozen study data or engine source changed.
- Text bounding boxes fit the horizontal text area and page boundaries.
  The full-document contact sheet and detailed renders were inspected. The
  main single-run summary and novelty tables fit intact on page 14; both
  single-run appendix tables fit on page 25. Appendix B starts on a fresh
  page so its complete 27-string frequency table stays intact on page 23.
- No undefined citations/references, missing-character warnings, or overfull
  boxes. Three nonfatal underfull vertical-box warnings and the previously
  documented style diagnostics remain; this is not a warning-free build.
- The source ZIP contains 18 files and is 61,645 bytes, including all ten
  generated tables. It rebuilds independently with identical extracted PDF
  text. The historical arithmetic ZIP remains unchanged at 294,696 bytes.
- ZIP integrity, safe-path, and private-identifier checks pass. Official style
  files still match the downloaded template byte for byte. The new study audit
  and investigation remain separate from the historical arithmetic ZIP;
  complete anonymous experimental packaging is still pending.

Issue 3 PDF SHA-256:

```text
5957e3c793434de4bee6f15482a5d4c7cd773f0fd2672795dda9b5296ea7cc86
```

Issue 3 source ZIP SHA-256:

```text
2e5229eef8a4f96f1d64c0e02f4c1595077fd4331218502f5c5ec2d44bfbecff
```

The analysis ZIP retains SHA-256
`c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52`.

## Previous Issue 2 Build

2026-09-07. The author-approved `misc3` discrepancy treatment is incorporated
in Section 7.1 (page 14) and Appendix B (pages 19-20), with corresponding
abstract, contribution, conclusion, and artifact-boundary updates. The draft
still awaits the remaining issues and final author review. No commit or push
is authorized.

- PDF: 21 US Letter pages, anonymous byline, empty author metadata, and 22
  embedded fonts. All 35 citations and bibliography entries remain present.
- All 27 academic tests pass, including saved-data investigation checks and
  generation of the complete 27-string frequency table. Counts use independently
  frozen prefixes, held-out reference episodes, and port episodes separately.
- The v3 export verifier reproduces all 25,974 saved episodes' statistics with
  zero engine executions. All 114 frozen source-file hashes still match.
- Text bounding boxes fit the horizontal text area and page boundaries.
  A full-document contact sheet and detailed renders of the case study and
  appendix were inspected. The frequency table fits intact on page 19.
- No undefined citations/references, missing-character warnings, or overfull
  boxes. Four nonfatal underfull vertical-box warnings and the previously
  documented style diagnostics remain; this is not a warning-free build.
- The source ZIP contains 14 files and is 54,457 bytes, including all six
  generated tables. It rebuilds independently with identical extracted PDF
  text. The historical arithmetic ZIP remains unchanged at 294,696 bytes.
- ZIP integrity, safe-path, and private-identifier checks pass. The official
  style files still match the downloaded template byte for byte. The new
  investigation's audit and event ledger remain separate from the historical
  arithmetic ZIP; anonymous experimental packaging is still pending.

Issue 2 PDF SHA-256:

```text
f7bf6fc3c87612a8e4350c97495cc1e13046bf93433522223daf197eb923700c
```

Issue 2 source ZIP SHA-256:

```text
d5689ddd1a48f4c4c553cf2b8f872208ebfe0a297b7a2a5ea126400079cb38a1
```

The analysis ZIP retains SHA-256
`c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52`.

## Previous Issue 1 Build

2026-09-07. The author-approved coverage/population-choice revision is built
with Pandoc 3.11 and Tectonic 0.17.0. This is an intermediate revision, not
the author's final approval or permission to commit.

- PDF: 18 US Letter pages, anonymous byline, empty author metadata, and 22
  embedded fonts. All 35 citations and bibliography entries remain present.
- The coverage table includes all 19 problems, including failed and skipped
  validation. Six new table-generation tests pass alongside the 14 existing
  arithmetic tests, for 20 tests in the repository's academic suite.
- The v3 export verifier reproduces all 25974 saved episodes' statistics
  without engine execution. No study data or engine source was modified.
- Text bounding boxes fit the page and horizontal text area. The full draft
  was scanned as a page contact sheet; the coverage section, table, population
  discussion, and conclusion were also checked in rendered page images.
- No undefined references/citations, missing-character warnings, or overfull
  boxes. Four nonfatal underfull vertical-box warnings remain, in addition to
  the previously documented style diagnostics. This is not a warning-free build.
- The source ZIP now contains 13 files, including the fifth generated table,
  and is 50639 bytes. It rebuilds independently with identical extracted PDF
  text. The historical arithmetic ZIP remains unchanged at 294696 bytes;
  it does not include the new episodic experimental bundle or coverage tests.
- Source/analysis ZIP integrity, safe paths, and private-identifier checks pass.
  Official style files still match the downloaded template byte for byte.

Issue 1 PDF SHA-256:

```text
9cc8901986ce05ef0832d163047f600aa128f4e66f15ceebe3cf4a3d07f86e5d
```

Issue 1 source ZIP SHA-256:

```text
8e054846309e657d71fbd5b8c83e3e6ff62d00ed4c2b70eede596dd664aaddd9
```

The analysis ZIP retains SHA-256
`c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52`.
The following record describes the earlier build, not the current page count
or source-ZIP contents.

## Previous Build Record

2026-09-06 UTC (2026-09-05 EDT). Amortization and expanded learning-mode revision
generated with Pandoc 3.11 and built with
Tectonic 0.17.0. The canonical source is [manuscript.md](manuscript.md).

## PDF Checks

- PDF: 16 pages, US Letter (612 by 792 points), PDF 1.5, unencrypted.
- All 22 used fonts are embedded; no bitmap/Type 3 fonts.
- All 35 bibliography entries are rendered; all 35 source citation keys are present.
- No undefined citations, unresolved section references, missing-glyph warnings, or overfull/underfull boxes in the final build.
- The byline is anonymous, PDF author metadata is empty, and PDF link annotations contain no identifying Petacat repository URL.
- Every rendered page was visually inspected, and all affected pages were re-rendered and checked after final text corrections. The episodic table fits on one page; the notation and benchmark appendices share a page with both tables intact and no orphaned heading. References start on a fresh page. Text bounding boxes were checked against the 6.5-inch horizontal text area and the page boundary.
- Official `tmlr.sty`, `tmlr.bst`, and `fancyhdr.sty` match the downloaded ZIP byte for byte. The historical dated Markdown is preserved with a superseded-draft notice; the new manuscript contains the substantive revisions.

Nonfatal compiler diagnostics remain: Tectonic reports a legacy UTF-8 byte in
its bundled `lineno.sty`, and `amsmath` reports the foreign command
`\atopwithdelims`. These did not produce a visible missing glyph or text
overflow. Some mathematical font glyphs have imperfect PDF text extraction;
mathematical expressions were checked visually. This is not a PDF/A or
accessibility certification.

## Standalone Bundle Checks

- The [source ZIP](support-set-oracles-tmlr-source.zip) contains 12 files
  (47,127 bytes): build instructions, `.tex`, `.bib`, `.bbl`, four generated tables, three
  unmodified style files, and the template license. It rebuilt successfully
  after extraction into a clean temporary directory, without Pandoc or the
  repository. Its rebuilt PDF has identical extracted text to the delivered PDF.
- The [analysis ZIP](support-set-oracles-analysis.zip) contains 12 files
  (294,696 bytes):
  instructions, data documentation, four unchanged archived JSON data files,
  two arithmetic reports, two audit scripts, and two test files. In a separate
  clean directory, the audits reproduced `number-audit.json` and
  `episode-audit.json` byte for byte and all 14 tests passed using only the
  Python standard library.
- Both archives passed ZIP integrity and path checks. Neither contains the
  named historical draft, public repository documentation, original Metacat
  source, or private machine/network identifiers. Both are below 100 MB.
- These are a manuscript build bundle and an arithmetic supplement, not a
  complete experimental code artifact or independent replication.

## Test Results

Run from the repository root:

```sh
python3 -m unittest discover -s academic/tests -p 'test_*.py'
python3 -m unittest discover -s Metacat/tests -p 'test_*.py'
.venv/bin/python -m pytest tests/unit/test_compare_harness.py tests/module/test_group_image_direction.py -q
```

Results: all 14 arithmetic tests passed for this revision and again in the
extracted bundle. The earlier 14 reconstruction/packaging tests and 23
targeted CPU tests passed before the remote study launched. That earlier
pytest process exited successfully but reported
a Metal-device-unavailable exception in an exit callback in the restricted
environment. No GPU execution or fresh stochastic engine results are claimed
by this document. The separately running remote study is recorded in
[STUDY-STATUS.md](STUDY-STATUS.md), and none of its unfinished results are used here.
Build and packaging commands are in [README.md](README.md).

The additional memory regression definitions described in
[EPISODIC-IMPLEMENTATION-REVIEW.md](EPISODIC-IMPLEMENTATION-REVIEW.md) were
inspected, not executed for this manuscript expansion. They are not included
in the count of 14 arithmetic tests or claimed as newly passing engine tests.

## SHA-256 Checksums

Template archive SHA-256:

```text
48f9f972cc812d2cfc8032a78578c2f5d8939b92fbd4f61095e8cbeb3394f92d
```

Final manuscript PDF SHA-256:

```text
2363f50bb8df3a3be2baef615c2ecd7ab1e1d63b15d505c858c19ef523bb13c2
```

Source ZIP SHA-256:

```text
36048ddb3de6c8a545377b7edd225c80680bdc16ae8128bfece670d76cb56505
```

Analysis ZIP SHA-256:

```text
c187cba8a86df98b858c082ae5f3d7db2e9ac2c04076d76b7baa7f195cb64e52
```

Build verification does not resolve the remaining scientific and provenance
gaps in [REVISION-STATUS.md](REVISION-STATUS.md). It does not establish
submission readiness. No OpenReview submission was made.
